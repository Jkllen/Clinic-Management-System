using System.Windows.Controls;

namespace CruzNeryClinic.Views
{
    // SecurityQuestionsView is the code-behind for the Security Questions screen.
    // This screen uses normal TextBox controls, so no special password handling is needed.
    public partial class SecurityQuestionsView : UserControl
    {
        public SecurityQuestionsView()
        {
            InitializeComponent();
        }
    }
}