using System.Windows.Controls;

namespace CruzNeryClinic.Views
{
    // ForgotPasswordView is the code-behind for the Forgot Password screen.
    // Since this screen has no PasswordBox, it does not need special code-behind logic.
    public partial class ForgotPasswordView : UserControl
    {
        public ForgotPasswordView()
        {
            InitializeComponent();
        }
    }
}