<UserControl
    x:Class="CruzNeryClinic.Views.SplashView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="900"
    d:DesignWidth="1600"
    mc:Ignorable="d">

    <!--
        SplashView
        This screen appears first when the system opens.
        It shows the Cruz-Nery Dental Clinic logo while the app initializes.
    -->

    <Grid>

        <!--
            Main background gradient.
            This copies the light-to-cyan style from your splash screen design.
        -->
        <Grid.Background>
            <!--
                Main background gradient.
                Starts from top-left white and ends at bottom-right cyan.
            -->
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Offset="0.00" Color="#FFFFFF" />
                <GradientStop Offset="1.00" Color="#91EFEF" />
            </LinearGradientBrush>
        </Grid.Background>

        <!--
            Center content area.
            The logo is centered both horizontally and vertically.
        -->
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">

            <!--  Clinic logo (animated: fade + pop-in, then a gentle float)  -->
            <Image
                Width="440"
                Height="440"
                Opacity="0"
                RenderOptions.BitmapScalingMode="HighQuality"
                RenderTransformOrigin="0.5,0.5"
                Source="/Assets/Images/cruz-nery-logo.png"
                Stretch="Uniform">

                <Image.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform x:Name="LogoScale" ScaleX="0.85" ScaleY="0.85" />
                        <TranslateTransform x:Name="LogoFloat" />
                    </TransformGroup>
                </Image.RenderTransform>

                <Image.Triggers>
                    <EventTrigger RoutedEvent="Image.Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <!--  Fade in  -->
                                <DoubleAnimation
                                    Storyboard.TargetProperty="Opacity"
                                    From="0" To="1"
                                    Duration="0:0:0.9" />

                                <!--  Scale pop-in (overshoot then settle)  -->
                                <DoubleAnimation
                                    Storyboard.TargetName="LogoScale"
                                    Storyboard.TargetProperty="ScaleX"
                                    From="0.85" To="1"
                                    Duration="0:0:0.9">
                                    <DoubleAnimation.EasingFunction>
                                        <BackEase Amplitude="0.4" EasingMode="EaseOut" />
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                                <DoubleAnimation
                                    Storyboard.TargetName="LogoScale"
                                    Storyboard.TargetProperty="ScaleY"
                                    From="0.85" To="1"
                                    Duration="0:0:0.9">
                                    <DoubleAnimation.EasingFunction>
                                        <BackEase Amplitude="0.4" EasingMode="EaseOut" />
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>

                                <!--  Gentle continuous float (up and down)  -->
                                <DoubleAnimation
                                    Storyboard.TargetName="LogoFloat"
                                    Storyboard.TargetProperty="Y"
                                    From="0" To="-14"
                                    BeginTime="0:0:0.9"
                                    Duration="0:0:1.8"
                                    AutoReverse="True"
                                    RepeatBehavior="Forever">
                                    <DoubleAnimation.EasingFunction>
                                        <SineEase EasingMode="EaseInOut" />
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Image.Triggers>
            </Image>

        </StackPanel>

    </Grid>
</UserControl>
