<UserControl x:Class="CruzNeryClinic.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:fa="clr-namespace:FontAwesome.Sharp;assembly=FontAwesome.Sharp"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="1600"
             d:DesignHeight="900">

    <!--
        LoginView
        This is the login screen of the system.
        The layout is wrapped in a Viewbox so the card scales down when the window becomes smaller.
    -->

    <UserControl.Resources>

        <!-- Converts bool values into Visibility for error messages. -->
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <!-- 
            Modern link button style.
            Used for Forgot Password.
        -->
        <Style x:Key="LinkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock x:Name="LinkText"
                                Text="{TemplateBinding Content}"
                                Foreground="{TemplateBinding Foreground}"
                                FontSize="{TemplateBinding FontSize}"
                                FontWeight="{TemplateBinding FontWeight}"/>

                        <ControlTemplate.Triggers>
                            <!-- Changes color and underlines text when hovered -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="LinkText" Property="Foreground" Value="#223357"/>
                                <Setter TargetName="LinkText" Property="TextDecorations" Value="Underline"/>
                            </Trigger>

                            <!-- Slight pressed feedback -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="LinkText" Property="Foreground" Value="#182641"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>

        <!-- 
            Background gradient based on Figma design:
            top-left #FFFFFF to bottom-right #91EFEF.
        -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FFFFFF" Offset="0.00"/>
                <GradientStop Color="#91EFEF" Offset="1.00"/>
            </LinearGradientBrush>
        </Grid.Background>

        <!-- 
            Viewbox makes the login card scale properly on smaller window sizes.
            It keeps the card proportions while allowing the screen to remain flexible.
        -->
        <Viewbox Stretch="Uniform"
                 StretchDirection="DownOnly"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center"
                 Margin="30">

            <!--
                This Grid is the original design canvas for the login card.
                The Viewbox scales this whole area down if needed.
            -->
            <Grid Width="560" Height="780" Opacity="0">

                <Grid.RenderTransform>
                    <TranslateTransform x:Name="LoginPanelSlide" Y="24" />
                </Grid.RenderTransform>

                <Grid.Triggers>
                    <EventTrigger RoutedEvent="Grid.Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <!--  Fade the login panel in  -->
                                <DoubleAnimation
                                    Storyboard.TargetProperty="Opacity"
                                    From="0" To="1"
                                    Duration="0:0:0.6" />
                                <!--  Subtle slide up while fading  -->
                                <DoubleAnimation
                                    Storyboard.TargetName="LoginPanelSlide"
                                    Storyboard.TargetProperty="Y"
                                    From="24" To="0"
                                    Duration="0:0:0.6">
                                    <DoubleAnimation.EasingFunction>
                                        <CubicEase EasingMode="EaseOut" />
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Grid.Triggers>

                <!-- Login card background -->
                <Border CornerRadius="36"
                        Background="#EAFDFD"
                        BorderBrush="White"
                        BorderThickness="1"
                        Padding="44"
                        SnapsToDevicePixels="True">

                    <Border.Effect>
                        <DropShadowEffect Color="#707070"
                                          BlurRadius="10"
                                          ShadowDepth="4"
                                          Opacity="0.28"/>
                    </Border.Effect>

                    <Grid>
                        <Grid.RowDefinitions>
                            <!-- 0: Logo area -->
                            <RowDefinition Height="285"/>

                            <!-- 1: Login title -->
                            <RowDefinition Height="Auto"/>
                            
                            <!-- 2: User ID title -->
                            <RowDefinition Height="Auto"/>

                            <!-- 3: User ID textbox -->
                            <RowDefinition Height="Auto"/>
                            
                            <!-- 4: Password title -->
                            <RowDefinition Height="Auto"/>

                            <!-- 5: Password textbox -->
                            <RowDefinition Height="Auto"/>

                            <!-- 6: Forgot password -->
                            <RowDefinition Height="Auto"/>

                            <!-- 7: Error message -->
                            <RowDefinition Height="25"/>

                            <!-- 8: Login button -->
                            <RowDefinition Height="78"/>

                            <!-- Extra bottom spacing -->
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Clinic logo -->
                        <Image Grid.Row="0"
                               Source="/Assets/Images/cruz-nery-logo.png"
                               Width="280"
                               Height="280"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Top"
                               Stretch="Uniform"
                               RenderOptions.BitmapScalingMode="HighQuality"/>

                        <!-- Login title -->
                        <TextBlock Grid.Row="1"
                                   Text="Login"
                                   FontFamily="{StaticResource AppFont}"
                                   FontSize="26"
                                   FontWeight="Bold"
                                   Foreground="#111111"
                                   Margin="0,0,0,12"/>
                        
                        <!-- User ID title -->
                        <TextBlock Grid.Row="2"
                                   Text="User ID"
                                   FontFamily="{StaticResource AppFont}"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="#111111"
                                   Margin="0,0,0,12"/>
                        
                        <!-- User ID input with FontAwesome user icon -->
                        <Grid Grid.Row="3"
                              Height="58"
                              Margin="0,0,0,18">

                            <TextBox Style="{StaticResource AuthIconTextBoxStyle}"
                                     Text="{Binding LoginInput, UpdateSourceTrigger=PropertyChanged}"
                                     Tag="Enter your user id"
                                     ToolTip="Enter your user ID"/>

                            <fa:IconBlock Icon="User"
                                          Foreground="#08CFA5"
                                          FontSize="26"
                                          Width="32"
                                          Height="32"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          Margin="18,2,0,0"/>
                        </Grid>
                        
                        <!-- Password title -->
                        <TextBlock Grid.Row="4"
                                   Text="Password"
                                   FontFamily="{StaticResource AppFont}"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="#111111"
                                   Margin="0,0,0,12"/>

                        <!-- Password input with FontAwesome lock and eye-slash icon -->
                        <Grid Grid.Row="5"
                            Height="58"
                            Margin="0,0,0,8">

                            <!-- Hidden password mode -->
                            <PasswordBox x:Name="PasswordInput"
                                        Style="{StaticResource AuthIconPasswordBoxStyle}"
                                        Tag="Enter your password"
                                        PasswordChanged="PasswordInput_PasswordChanged"
                                        ToolTip="Enter your password"/>

                            <!-- Visible password mode -->
                            <TextBox x:Name="VisiblePasswordInput"
                                    Style="{StaticResource AuthVisiblePasswordTextBoxStyle}"
                                    Tag="Enter your password"
                                    TextChanged="VisiblePasswordInput_TextChanged"
                                    Visibility="Collapsed"
                                    ToolTip="Enter your password"/>

                            <!-- Lock icon -->
                            <fa:IconBlock Icon="Lock"
                                        Foreground="#08CFA5"
                                        FontSize="25"
                                        Width="32"
                                        Height="32"
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Center"
                                        Margin="18,2,0,0"/>

                            <!-- Functional show/hide password button -->
                            <Button x:Name="TogglePasswordButton"
                                    Width="42"
                                    Height="42"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    Margin="0,2,8,0"
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                    Cursor="Hand"
                                    Click="TogglePasswordButton_Click"
                                    Style="{StaticResource IconButtonStyle}">

                                <fa:IconBlock x:Name="PasswordEyeIcon"
                                            Icon="EyeSlash"
                                            Foreground="#666666"
                                            FontSize="22"
                                            Width="32"
                                            Height="32"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Margin="0,5,0,0"/>
                            </Button>
                        </Grid>

                        <!-- Forgot password button -->
                        <Button Grid.Row="6"
                                Content="Forgot Password?"
                                Command="{Binding ForgotPasswordCommand}"
                                HorizontalAlignment="Right"
                                Background="Transparent"
                                BorderBrush="Transparent"
                                Foreground="Black"
                                FontSize="16"
                                FontWeight="SemiBold"
                                Cursor="Hand"
                                Padding="0"
                                Margin="0,2,0,20"
                                Style="{StaticResource LinkButtonStyle}"/>

                        <!-- Error message -->
                        <TextBlock Grid.Row="7"
                                   Text="{Binding ErrorMessage}"
                                   Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}"
                                   Foreground="#C62828"
                                   FontSize="15"
                                   FontWeight="SemiBold"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>

                        <!-- Login button -->
                        <Button Grid.Row="8"
                                Content="Login"
                                Command="{Binding LoginCommand}"
                                Width="170"
                                Height="56"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Background="#223357"
                                Foreground="White"
                                FontFamily="{StaticResource AppFont}"
                                FontSize="28"
                                Margin="0,5,0,5"
                                FontWeight="Bold"
                                Style="{StaticResource PrimaryButtonStyle}"/>
                    </Grid>
                </Border>
            </Grid>
        </Viewbox>
    </Grid>
</UserControl>
